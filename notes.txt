TODO:
- drop repeats so things only update when they need to
- abstract shared functions
- refactor to send payload objects
- work out the storing of hue -- * / / by 360 when and where.


-----------------------------------------------------------

get things to return literally just the sub-value
handle state only in the parent
pass it down
